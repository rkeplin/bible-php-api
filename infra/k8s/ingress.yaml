apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bible-php-api
  namespace: bible
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - k8s-bible-php-api.rkeplin.com
        - bible-php-api.rkeplin.com
      secretName: k8s-bible-php-api-tls
  rules:
    - host: k8s-bible-php-api.rkeplin.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bible-php-api
                port:
                  number: 80
    - host: bible-php-api.rkeplin.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bible-php-api
                port:
                  number: 80
